# Fitness API - REST API для трекинга тренировок

> **Pet-проект** - учебный проект для практики работы с FastAPI и современными технологиями веб-разработки.

REST API для отслеживания тренировок в тренажерном зале и волейболе. Проект включает полный набор CRUD-операций для управления тренировочными сессиями, статистику по периодизации, JWT-аутентификацию и валидацию данных.

## 🎯 Основные возможности

- **CRUD-операции** над тренировочными сессиями (workouts)
- **Типы тренировок**: тренажерный зал (gym) и волейбол (volleyball)
- **Статистика**: агрегированные данные по периодизации тренировок
- **Аутентификация**: JWT-токены для безопасного доступа
- **Валидация**: строгая валидация данных с помощью SQLModel (основан на Pydantic)
- **Фильтрация и пагинация**: эффективная работа с большими объемами данных
- **База данных**: поддержка SQLite (для разработки) и PostgreSQL (для продакшена)

## 🛠 Технологический стек

- **FastAPI** - современный веб-фреймворк для создания API
- **SQLModel** - ORM для работы с базой данных (объединяет SQLAlchemy и Pydantic)
- **SQLAlchemy** - ORM для работы с базой данных
- **Pydantic** - валидация данных и сериализация
- **Python-JOSE** - работа с JWT-токенами
- **Passlib** - хеширование паролей (bcrypt)
- **Uvicorn** - ASGI-сервер для запуска приложения
- **uv** - современный менеджер пакетов Python

## 📋 Требования

- Python 3.13+
- uv (менеджер пакетов)

## 🚀 Установка и запуск

### Инициализация проекта

```bash
uv init fitness-api --python 3.13
```

### Установка зависимостей

```bash
uv sync
```

Эта команда установит все зависимости, указанные в `pyproject.toml`.

### Запуск приложения

```bash
uv run uvicorn app.main:app --reload
```

Приложение будет доступно по адресу: `http://localhost:8000`

Документация API (Swagger UI): `http://localhost:8000/docs`  
Альтернативная документация (ReDoc): `http://localhost:8000/redoc`

**Примечание:** Все API эндпоинты доступны под префиксом `/api` (например, `/api/workouts`)

## 📡 API Эндпоинты

### Тренировочные сессии (Workouts)

#### Создание тренировочной сессии

```
POST /api/workouts
```

Создает новую тренировочную сессию. Параметры:

- `type` - тип тренировки (gym/volleyball)
- `duration` - длительность тренировки (в минутах)
- `repetitions` - количество повторений
- `planned_date` - запланированная дата тренировки
- `exercises` - список упражнений (опционально)
- `notes` - дополнительные заметки (опционально)

#### Получение списка тренировок

```
GET /api/workouts
```

Возвращает список всех тренировочных сессий с поддержкой:

- **Фильтрации** через query parameters:
  - `type` - фильтр по типу тренировки
  - `date_from` / `date_to` - фильтр по дате
  - `min_duration` / `max_duration` - фильтр по длительности
- **Пагинации**:
  - `page` - номер страницы
  - `size` - количество элементов на странице

#### Получение конкретной тренировки

```
GET /api/workouts/{workout_id}
```

Возвращает детальную информацию о тренировке по ID.

#### Обновление тренировки

```
PUT /api/workouts/{workout_id}
```

Обновляет данные существующей тренировочной сессии.

#### Удаление тренировки

```
DELETE /api/workouts/{workout_id}
```

Удаляет тренировочную сессию по ID.

### Статистика

#### Получение статистики

```
GET /api/stats
```

Возвращает агрегированную статистику по тренировкам:

- Общее количество тренировок (`global_trains_amount`)
- Общая длительность (`global_trains_duration`)
- Статистика по типам тренировок (`global_trains_by_type`) - опционально, если передан параметр `type`
- Статистика по дате (`global_trains_by_date`) - опционально, если передан параметр `date`

Параметры запроса:

- `type` (опционально) - фильтр по типу тренировки (gym/volleyball)
- `date` (опционально) - фильтр по дате тренировки

### Аутентификация

#### Регистрация

```
POST /api/auth/register
```

Регистрация нового пользователя.

#### Вход

```
POST /api/auth/login
```

Авторизация пользователя, возвращает JWT-токен и устанавливает его в cookie.

#### Выход

```
POST /api/auth/logout
```

Выход из системы (удаление токена из cookie).

#### Получение информации о текущем пользователе

```
GET /api/auth/me
```

Возвращает информацию о текущем аутентифицированном пользователе.

## 🗄 База данных

Проект использует SQLModel (который основан на SQLAlchemy) для работы с базой данных. Поддерживаются:

- **SQLite** - для разработки и тестирования (по умолчанию)
- **PostgreSQL** - для продакшена (можно настроить через переменную окружения `DATABASE_URL`)

Таблицы создаются автоматически при запуске приложения через `SQLModel.metadata.create_all()`.

## 🔐 Безопасность

- JWT-токены для аутентификации
- Хеширование паролей с помощью bcrypt
- Валидация всех входящих данных через Pydantic
- Защита эндпоинтов через зависимости FastAPI

## 🏗 Архитектура проекта

Проект использует **Layered Architecture (Слоистая архитектура)** для четкого разделения ответственности:

```
fitness-api-pet/
├── app/
│   ├── main.py              # Точка входа, создание FastAPI app
│   ├── core/                # Конфигурация и настройки
│   │   ├── config.py        # Настройки приложения
│   │   └── database.py      # Подключение к БД, сессии
│   ├── api/                 # API слой (роутеры)
│   │   ├── deps.py          # Зависимости FastAPI
│   │   └── routers/         # Роутеры по доменам
│   │       ├── workouts.py  # Эндпоинты для тренировок
│   │       ├── stats.py     # Эндпоинты для статистики
│   │       └── auth.py      # Эндпоинты для аутентификации
│   ├── models/              # SQLModel модели (ORM + Pydantic схемы)
│   │   ├── workout.py
│   │   └── user.py
│   ├── repositories/        # Repository Pattern (работа с БД)
│   │   ├── workout_repository.py
│   │   ├── user_repository.py
│   │   ├── auth_repository.py
│   │   └── stats_repository.py
│   ├── services/            # Бизнес-логика
│   │   ├── workout_service.py
│   │   ├── stats_service.py
│   │   ├── auth_service.py
│   │   └── user_service.py
│   ├── security/            # Утилиты безопасности
│   │   ├── create_access_token.py
│   │   ├── get_user_id_by_token.py
│   │   ├── hash_password.py
│   │   └── cookie_utils.py
│   └── utils/               # Вспомогательные утилиты
│       ├── date_utils.py
│       └── db_decorator.py
```

### Принципы архитектуры и ответственность слоев:

Проект следует принципам **SOLID**, **KISS** и **DRY** для обеспечения чистого и поддерживаемого кода.

#### 📡 **API Layer (routers)** - `app/api/routers/`

**Ответственность:**

- Обработка HTTP-запросов и ответов
- Валидация входных данных через SQLModel модели (которые основаны на Pydantic)
- Вызов сервисов для выполнения бизнес-логики
- Формирование HTTP-ответов
- Управление cookies и headers
- Обработка ошибок на уровне HTTP

**Что НЕ должен делать:**

- Содержать бизнес-логику
- Работать напрямую с базой данных
- Генерировать токены или выполнять криптографические операции

**Пример:**

```python
@router.post("/login")
async def login(username: str, password: str, response: Response):
    token = auth_service.login(username, password)  # Вызов сервиса
    if token:
        set_auth_cookie(response, token)  # Управление HTTP-ответом
        return {"message": "Успешный вход"}
```

---

#### 🧠 **Service Layer** - `app/services/`

**Ответственность:**

- Бизнес-логика приложения
- Координация работы между репозиториями
- Валидация бизнес-правил
- Трансформация данных между слоями
- Генерация токенов (через утилиты)
- Агрегация данных из нескольких источников
- Вычисления и расчеты (статистика, аналитика)

**Что НЕ должен делать:**

- Работать напрямую с HTTP-запросами/ответами
- Выполнять прямые SQL-запросы или операции с БД
- Содержать детали реализации работы с базой данных

**Пример:**

```python
def login(self, username: str, password: str) -> str | None:
    user = auth_repository.login(username, password)  # Работа через репозиторий
    if user:
        return create_access_token(user.id)  # Генерация токена
    return None
```

---

#### 💾 **Repository Layer** - `app/repositories/`

**Ответственность:**

- Абстракция работы с источником данных (БД, файлы, память)
- CRUD-операции (Create, Read, Update, Delete)
- Поиск и фильтрация данных
- Возврат доменных моделей (User, Workout и т.д.)
- Инкапсуляция SQL-запросов или операций с данными

**Что НЕ должен делать:**

- Содержать бизнес-логику
- Генерировать токены или выполнять криптографические операции
- Валидировать бизнес-правила
- Работать с HTTP-запросами/ответами
- Выполнять сложные вычисления или агрегации

**Пример:**

```python
@with_db_session()
def login(self, db: Session, username: str, password: str) -> User | None:
    """Только проверка учетных данных и возврат пользователя"""
    user = db.query(User).filter(User.username == username).first()
    if user and verify_password(password, user.password):
        return user  # Возврат доменной модели
    return None
```

---

#### 🔐 **Security Layer** - `app/security/`

**Ответственность:**

- Генерация и валидация JWT токенов
- Хеширование и проверка паролей
- Работа с cookies (установка, удаление)
- Декодирование токенов
- Криптографические операции

**Принцип:** Переиспользуемые утилиты без состояния (pure functions где возможно)

**Пример:**

```python
def create_access_token(user_id: int) -> str:
    """Генерация JWT токена - чистая функция"""
    expire = datetime.utcnow() + timedelta(minutes=settings.ACCESS_TOKEN_EXPIRE_MINUTES)
    payload = {"user_id": user_id, "exp": expire}
    return jwt.encode(payload, settings.SECRET_KEY, algorithm=settings.ALGORITHM)
```

---

---

#### 🗄️ **Models** - `app/models/`

**Ответственность:**

- SQLModel модели (объединяют SQLAlchemy ORM и Pydantic схемы)
- Определение структуры таблиц БД
- Валидация входных данных (request)
- Сериализация выходных данных (response)
- Связи между таблицами (relationships)
- Маппинг между БД и Python объектами

---

### 🔄 Поток данных в приложении:

```
HTTP Request
    ↓
API Router (валидация, HTTP-обработка)
    ↓
Service (бизнес-логика, координация)
    ↓
Repository (работа с данными)
    ↓
Database / Data Source
```

**Обратный поток:**

```
Database
    ↓
Repository (возврат моделей)
    ↓
Service (трансформация, бизнес-логика)
    ↓
API Router (сериализация в схемы)
    ↓
HTTP Response
```

### ✅ Преимущества такой архитектуры:

- **Разделение ответственности (SRP)** - каждый слой отвечает за свою задачу
- **Тестируемость** - легко мокировать репозитории и тестировать сервисы
- **Переиспользование** - сервисы можно использовать из разных роутеров
- **Гибкость** - легко заменить источник данных (БД → файлы → API)
- **Поддерживаемость** - изменения в одном слое не затрагивают другие

## 📊 Структура данных

### Тренировочная сессия (Workout)

- `id` - уникальный идентификатор
- `user_id` - связь с пользователем
- `type` - тип тренировки (gym/volleyball)
- `duration` - длительность в минутах
- `repetitions` - количество повторений
- `planned_date` - запланированная дата тренировки (опционально)
- `exercises` - список упражнений (опционально)
- `notes` - дополнительные заметки (опционально)

## 🎓 Цель проекта

Этот проект идеально подходит для практики:

- Работы с SQLModel (объединение SQLAlchemy и Pydantic)
- Реализации JWT-аутентификации
- Валидации данных с Pydantic
- Создания RESTful API с FastAPI
- Реализации фильтрации и пагинации
- Работы с SQLite и PostgreSQL
- Реализации Repository Pattern
- Слоистой архитектуры (Layered Architecture)

## 📝 Лицензия

MIT
